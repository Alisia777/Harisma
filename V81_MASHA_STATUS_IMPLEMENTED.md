# V81 · источник статусов Маши встроен в портал

Что добавлено:
- новый источник `data/masha_status_updates.json`;
- WB-статусы РОПа встроены в реестр SKU;
- в карточке SKU появился блок `Статус от РОПа / Маши`;
- статусный источник встроен в контроль задач без дублирования: задача создаётся только для action-item строк;
- в executive-слой добавлен status summary по WB;
- source не выведен отдельной вкладкой, а встроен в существующие блоки.

Что важно:
- seed-строки не создают авто-задачи, чтобы не дублировать текущие сигналы портала;
- когда Женя подключит реальный файл-источник, action-item строки с `needsTask=true` начнут попадать в общий задачник.
