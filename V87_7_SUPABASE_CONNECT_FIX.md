Что исправлено:
- портал больше не уходит сразу в локальный режим, если не загрузилась библиотека Supabase с jsDelivr;
- app.js теперь пытается подгрузить Supabase client по очереди из нескольких CDN;
- config.js грузится с cache-busting query;
- app.js и остальные модули тоже грузятся с cache-busting query;
- если один CDN недоступен, портал пробует следующий и только потом уходит в local fallback.

Что заменить в репозитории:
- index.html
- config.js
- app.js

Что не трогать:
- CNAME
- data/, если данные уже актуальны
