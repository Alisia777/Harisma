function renderControlCenter(){const e=document.getElementById("view-control");state.controlFilters.platform=normalizeControlWorkstreamFilter(state.controlFilters.platform);const t=filteredControlTasks(),n=filteredControlTasks({ignorePlatform:!0}),a=state.controlFilters.platform,s=buildControlWorkstreamSummary(n,a),i=[...new Set(getAllTasks().map(e=>e.owner||"Без owner"))].sort((e,t)=>e.localeCompare(t,"ru")),o=ownerOptions(),l=[...state.skus].filter(e=>!e?.flags?.assigned).sort((e,t)=>(t.focusScore||0)-(e.focusScore||0)||monthRevenue(t)-monthRevenue(e)).slice(0,8),r=[...state.storage.decisions||[]].filter(e=>"waiting_decision"===e.status||"new"===e.status).sort((e,t)=>(e.due||"9999-12-31").localeCompare(t.due||"9999-12-31")).slice(0,8),d=CONTROL_WORKSTREAM_FILTER_ORDER.map(e=>buildControlWorkstreamSummary(n,e)),c="all"===a?CONTROL_WORKSTREAM_ORDER.filter(e=>d.find(t=>t.key===e)?.tasks.length):[a],v=d.map(e=>renderControlWorkstreamCard(e,a)).join(""),p=c.length?c.map(e=>renderControlWorkstreamSection(d.find(t=>t.key===e))).join(""):'<div class="card" style="margin-top:14px"><div class="empty">Нет задач под текущий фильтр.</div></div>',m={active:t.filter(isTaskActive).length,overdue:t.filter(isTaskOverdue).length,noOwner:t.filter(e=>isTaskActive(e)&&!e.owner).length,waiting:t.filter(e=>"waiting_decision"===e.status).length,critical:t.filter(e=>isTaskActive(e)&&"critical"===e.priority).length,auto:t.filter(e=>"auto"===e.source&&isTaskActive(e)).length},u=t.slice(0,4),g=getRecentTaskHistory(t,8),$=l.length?l.map(e=>`\n    <div class="assign-row">\n      <div class="head">\n        <div>\n          <strong>${linkToSku(e.articleKey,e.article||e.articleKey)}</strong>\n          <div class="muted small">${escapeHtml(e.name||"Без названия")}</div>\n        </div>\n        <div class="badge-stack">${scoreChip(e.focusScore||0)}${skuOperationalStatus(e)}</div>\n      </div>\n      <div class="team-note">${escapeHtml(e.focusReasons||"Нужно просто закрепить owner и первый срок апдейта.")}</div>\n      <div class="inline-form" style="margin-top:10px">\n        <input class="inline-input" list="ownerOptionsList" data-owner-assign-input="${escapeHtml(e.articleKey)}" placeholder="Кто владелец SKU">\n        <input class="inline-input" data-owner-assign-role="${escapeHtml(e.articleKey)}" placeholder="Роль / зона" value="Owner SKU">\n        <button class="btn small-btn" type="button" data-save-owner="${escapeHtml(e.articleKey)}">Закрепить</button>\n      </div>\n    </div>\n  `).join(""):'<div class="empty">Все SKU уже закреплены</div>',k=r.length?r.map(e=>{const t=getSku(e.articleKey);return`\n      <div class="decision-item">\n        <div class="head">\n          <div>\n            <strong>${escapeHtml(e.title)}</strong>\n            <div class="muted small">${t?linkToSku(t.articleKey,t.article||t.articleKey):escapeHtml(e.articleKey)}</div>\n          </div>\n          <div class="badge-stack">${taskStatusBadge(e)}${e.owner?badge(e.owner,"info"):""}</div>\n        </div>\n        <div class="muted small">${escapeHtml(e.decision||"Нужно зафиксировать решение")}</div>\n        <div class="meta-line" style="margin-top:8px"><span class="muted small">Срок ${escapeHtml(e.due||"—")}</span><span class="muted small">${escapeHtml(e.createdBy||"Команда")}</span></div>\n      </div>\n    `}).join(""):'<div class="empty">Нет решений в ожидании</div>',h=g.length?g.map(e=>{const t=getTask(e.taskId);return`\n        <div class="decision-item">\n          <div class="head">\n            <div>\n              <strong>${escapeHtml(taskHeadline(t))}</strong>\n              <div class="muted small">${t?taskEntityLine(t,getSku(t.articleKey)):escapeHtml(e.taskId)}</div>\n            </div>\n            <div class="badge-stack">${taskHistoryBadge(e.kind)}${e.team?badge(e.team,"info"):""}</div>\n          </div>\n          <div class="muted small">${escapeHtml(e.text||"—")}</div>\n          <div class="meta-line" style="margin-top:8px"><span class="muted small">${fmt.date(e.createdAt)}</span><span class="muted small">${escapeHtml(e.author||"Команда")}</span></div>\n        </div>\n      `}).join(""):'<div class="empty">История появится после первых апдейтов по задачам</div>';e.innerHTML=`\n    <div class="section-title">\n      <div>\n        <h2>Контур задач по РОПам</h2>\n        <p>Сверху держим общую картину по выбранной площадке, ниже работаем уже по конкретным задачам, комментариям и развилкам.</p>\n      </div>\n      <div class="quick-actions">\n        <button class="quick-chip" data-control-preset="active">Активные</button>\n        <button class="quick-chip" data-control-preset="overdue">Просроченные</button>\n        <button class="quick-chip" data-control-preset="critical">Критичные</button>\n        <button class="quick-chip" data-control-preset="no_owner">Без owner</button>\n      </div>\n    </div>\n\n    <div class="kpi-strip">\n      <div class="mini-kpi"><span>Активно</span><strong>${fmt.int(m.active)}</strong></div>\n      <div class="mini-kpi danger"><span>Просрочено</span><strong>${fmt.int(m.overdue)}</strong></div>\n      <div class="mini-kpi warn"><span>Критично</span><strong>${fmt.int(m.critical)}</strong></div>\n      <div class="mini-kpi warn"><span>Без owner</span><strong>${fmt.int(m.noOwner)}</strong></div>\n      <div class="mini-kpi"><span>Ждёт решения</span><strong>${fmt.int(m.waiting)}</strong></div>\n      <div class="mini-kpi"><span>Авто-сигналы</span><strong>${fmt.int(m.auto)}</strong></div>\n    </div>\n\n    <div class="two-col" style="margin-top:14px">\n      <div class="card">\n        <div class="section-subhead">\n          <div>\n            <h3>${escapeHtml(controlWorkstreamMeta(s.key).label)}</h3>\n            <p class="small muted">${escapeHtml(controlWorkstreamMeta(s.key).description)}</p>\n          </div>\n          <div class="badge-stack">\n            ${badge(`${fmt.int(s.activeCount)} активн.`,s.activeCount?controlWorkstreamMeta(s.key).kind:"")}\n            ${s.overdueCount?badge(`${fmt.int(s.overdueCount)} проср.`,"danger"):badge("без просрочек","ok")}\n            ${s.waitingCount?badge(`${fmt.int(s.waitingCount)} ждут решения`,"warn"):badge("решения не висят","ok")}\n          </div>\n        </div>\n        <div class="task-mini-grid">\n          <div class="task-mini">\n            <div class="left"><strong>Ключевые owner</strong><div class="muted small">${escapeHtml(s.ownerPreview.length?s.ownerPreview.join(" · "):"Пока без явных owner")}</div></div>\n            <div class="badge-stack">${badge(`${fmt.int(s.typeCount)} типов задач`)}</div>\n          </div>\n          <div class="task-mini">\n            <div class="left"><strong>Фокус по контуру</strong><div class="muted small">${escapeHtml(s.criticalCount?"Сначала критичные и блокирующие задачи":"Можно идти по плановой очереди")}</div></div>\n            <div class="badge-stack">${badge(`${fmt.int(s.criticalCount)} крит.`,s.criticalCount?"danger":"ok")}</div>\n          </div>\n          <div class="task-mini">\n            <div class="left"><strong>Что делать менеджеру</strong><div class="muted small">${escapeHtml(s.waitingCount?"Разобрать задачи, которые зависли на согласовании":"Открывать задачу, обновлять шаг и фиксировать комментарий")}</div></div>\n            <div class="badge-stack">${badge("drilldown → комментарий → отчёт","info")}</div>\n          </div>\n        </div>\n      </div>\n\n      <div class="card">\n        <div class="section-subhead">\n          <div>\n            <h3>Поставить общую задачу</h3>\n            <p class="small muted">Для межкомандных задач без привязки к одному SKU: weekly, запуск, маркетинг, согласование, правки.</p>\n          </div>\n          ${badge("общий контур","info")}\n        </div>\n        <form id="generalTaskForm" class="form-grid compact">\n          <input name="title" placeholder="Что нужно сделать" required>\n          <input name="entityLabel" placeholder="Проект / тема / блок" value="${"all"!==a?controlWorkstreamMeta(a).label:""}">\n          <select name="platform">\n            <option value="cross" ${"cross"===a||"all"===a?"selected":""}>Общий контур</option>\n            <option value="wb" ${"wb"===a?"selected":""}>РОП WB</option>\n            <option value="ozon" ${"ozon"===a?"selected":""}>РОП Ozon</option>\n            <option value="retail" ${"retail"===a?"selected":""}>ЯМ / Летуаль / Магнит / ЗЯ</option>\n          </select>\n          <select name="type">\n            <option value="general">Общее</option>\n            <option value="launch">Новинка / запуск</option>\n            <option value="traffic">Трафик / продвижение</option>\n            <option value="content">Контент / карточка</option>\n            <option value="assignment">Закрепление</option>\n          </select>\n          <select name="priority">\n            ${Object.entries(PRIORITY_META).map(([e,t])=>`<option value="${e}" ${"high"===e?"selected":""}>${escapeHtml(t.label)}</option>`).join("")}\n          </select>\n          <input name="owner" list="ownerOptionsList" placeholder="Кто ведёт задачу">\n          <input name="due" type="date" value="${plusDays(2)}">\n          <textarea name="nextAction" rows="3" placeholder="Что считаем первым шагом и как поймём, что задача сделана" required></textarea>\n          <button class="btn primary" type="submit">Поставить задачу</button>\n        </form>\n      </div>\n    </div>\n\n    <div class="grid cards" style="margin-top:14px">${v}</div>\n\n    <div class="control-filters">\n      <input id="controlSearchInput" placeholder="Поиск по SKU, задаче, owner, контуру…" value="${escapeHtml(state.controlFilters.search)}">\n      <select id="controlPlatformFilter">\n        <option value="all" ${"all"===a?"selected":""}>Все контуры</option>\n        <option value="ozon" ${"ozon"===a?"selected":""}>РОП Ozon</option>\n        <option value="wb" ${"wb"===a?"selected":""}>РОП WB</option>\n        <option value="retail" ${"retail"===a?"selected":""}>ЯМ / Летуаль / Магнит / ЗЯ</option>\n        <option value="cross" ${"cross"===a?"selected":""}>Общий контур</option>\n      </select>\n      <select id="controlOwnerFilter">\n        <option value="all">Все owner</option>\n        ${i.map(e=>`<option value="${escapeHtml(e)}" ${state.controlFilters.owner===e?"selected":""}>${escapeHtml(e)}</option>`).join("")}\n      </select>\n      <select id="controlStatusFilter">\n        <option value="active" ${"active"===state.controlFilters.status?"selected":""}>Только активные</option>\n        <option value="all" ${"all"===state.controlFilters.status?"selected":""}>Все статусы</option>\n        ${Object.entries(TASK_STATUS_META).map(([e,t])=>`<option value="${e}" ${state.controlFilters.status===e?"selected":""}>${escapeHtml(t.label)}</option>`).join("")}\n      </select>\n      <select id="controlTypeFilter">\n        <option value="all">Все типы</option>\n        ${Object.entries(TASK_TYPE_META).map(([e,t])=>`<option value="${e}" ${state.controlFilters.type===e?"selected":""}>${escapeHtml(t)}</option>`).join("")}\n      </select>\n      <select id="controlHorizonFilter">\n        <option value="all">Весь горизонт</option>\n        <option value="overdue" ${"overdue"===state.controlFilters.horizon?"selected":""}>Просрочено</option>\n        <option value="today" ${"today"===state.controlFilters.horizon?"selected":""}>Срок сегодня</option>\n        <option value="week" ${"week"===state.controlFilters.horizon?"selected":""}>Срок на 7 дней</option>\n        <option value="no_owner" ${"no_owner"===state.controlFilters.horizon?"selected":""}>Без owner</option>\n      </select>\n      <select id="controlSourceFilter">\n        <option value="all">Все источники</option>\n        <option value="manual" ${"manual"===state.controlFilters.source?"selected":""}>Ручные + seed</option>\n        <option value="auto" ${"auto"===state.controlFilters.source?"selected":""}>Только авто-сигналы</option>\n      </select>\n    </div>\n\n    ${p}\n\n    <div class="team-strip">\n      <div class="card">\n        <div class="section-subhead">\n          <div>\n            <h3>Последние апдейты по задачам</h3>\n            <p class="small muted">Это лента истории: кто обновил задачу, что поменялось и где уже есть отчёт по результату.</p>\n          </div>\n          ${badge(`${fmt.int(g.length)} записей`,g.length?"info":"ok")}\n        </div>\n        <div class="decision-list">${h}</div>\n      </div>\n\n      <div class="card">\n        <div class="section-subhead">\n          <div>\n            <h3>Командный контур и закрепление</h3>\n            <p class="small muted">Отсюда быстро добиваем SKU без owner и держим общий контур в одном месте.</p>\n          </div>\n          ${badge("ready"===state.team.mode?"Supabase ready":"local"===state.team.mode?"local":state.team.mode,"ready"===state.team.mode?"ok":"error"===state.team.mode?"danger":"warn")}\n        </div>\n        <div class="team-note">${escapeHtml(state.team.note||"Локальный режим")} · ${escapeHtml(teamMemberLabel())}</div>\n        <datalist id="ownerOptionsList">${o.map(e=>`<option value="${escapeHtml(e)}"></option>`).join("")}</datalist>\n        <div class="assign-list" style="margin-top:12px">${$}</div>\n      </div>\n    </div>\n\n    <div class="check-grid" style="margin:14px 0">\n      <div class="card">\n        <div class="section-subhead">\n          <div>\n            <h3>Решения ждут подтверждения</h3>\n            <p class="small muted">То, что руководитель или бренд-лид должны быстро зафиксировать.</p>\n          </div>\n          ${badge(`${fmt.int(r.length)} шт.`,r.length?"warn":"ok")}\n        </div>\n        <div class="decision-list">${k}</div>\n      </div>\n\n      <div class="card">\n        <h3>Чек-лист контроля</h3>\n        <div class="check-list">\n          <div class="check-item"><strong>1.</strong><span>Закрыть просрочки и перенести сроки, если реально ждём другой отдел.</span></div>\n          <div class="check-item"><strong>2.</strong><span>Проверить все задачи без owner и закрепить их.</span></div>\n          <div class="check-item"><strong>3.</strong><span>Открывать задачу через карточку, а не только менять статус в списке.</span></div>\n          <div class="check-item"><strong>4.</strong><span>При закрытии просить короткий отчёт: что сделали, какой результат, где артефакт.</span></div>\n        </div>\n      </div>\n      <div class="card">\n        <h3>Что здесь уже контролируем</h3>\n        <div class="task-mini-grid">\n          ${u.length?u.map(renderMiniTask).join(""):'<div class="empty">Нет задач для экспресс-чека</div>'}\n        </div>\n      </div>\n    </div>\n  `,document.getElementById("controlSearchInput").addEventListener("input",e=>{state.controlFilters.search=e.target.value,renderControlCenter()}),e.querySelectorAll("[data-control-workstream]").forEach(e=>e.addEventListener("click",()=>{state.controlFilters.platform=e.dataset.controlWorkstream,renderControlCenter()})),document.getElementById("controlOwnerFilter").addEventListener("change",e=>{state.controlFilters.owner=e.target.value,renderControlCenter()}),document.getElementById("controlStatusFilter").addEventListener("change",e=>{state.controlFilters.status=e.target.value,renderControlCenter()}),document.getElementById("controlTypeFilter").addEventListener("change",e=>{state.controlFilters.type=e.target.value,renderControlCenter()}),document.getElementById("controlPlatformFilter").addEventListener("change",e=>{state.controlFilters.platform=e.target.value,renderControlCenter()}),document.getElementById("controlHorizonFilter").addEventListener("change",e=>{state.controlFilters.horizon=e.target.value,renderControlCenter()}),document.getElementById("controlSourceFilter").addEventListener("change",e=>{state.controlFilters.source=e.target.value,renderControlCenter()}),document.getElementById("generalTaskForm").addEventListener("submit",async e=>{e.preventDefault();const t=new FormData(e.currentTarget),n=await createManualTask({articleKey:"",entityLabel:t.get("entityLabel"),title:t.get("title"),type:t.get("type"),priority:t.get("priority"),platform:t.get("platform"),owner:t.get("owner"),due:t.get("due"),nextAction:t.get("nextAction")});renderControlCenter(),n?.id&&openTaskModal(n.id)}),e.querySelectorAll("[data-save-owner]").forEach(t=>t.addEventListener("click",async()=>{const n=t.dataset.saveOwner,a=e.querySelector(`[data-owner-assign-input="${n}"]`),s=e.querySelector(`[data-owner-assign-role="${n}"]`);await upsertOwnerAssignment({articleKey:n,ownerName:a?.value||"",ownerRole:s?.value||"Owner SKU",note:"Закреплено из контрольного центра"}),renderControlCenter(),state.activeSku===n&&renderSkuModal(n),rerenderCurrentView()}))}function closeTaskModal(){document.getElementById("taskModal")?.classList.remove("open"),state.activeTaskId=null}function openTaskModal(e){renderTaskModal(e)}