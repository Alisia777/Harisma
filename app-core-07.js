function renderSkuModal(e){const t=getSku(e);if(!t)return;state.activeSku=e;const n=document.getElementById("skuModalBody"),a=document.getElementById("skuModal"),s=getSkuComments(e),i=getSkuDecisions(e),r=getSkuControlTasks(e),l=nextTaskForSku(e),o=ownerOptions();n.innerHTML=`\n    <div class="modal-head">\n      <div>\n        <div class="muted small">${escapeHtml(t.brand||"Алтея")} · ${escapeHtml(t.segment||t.category||"—")}</div>\n        <h2>${escapeHtml(t.name||"Без названия")}</h2>\n        <div class="badge-stack">${linkToSku(t.articleKey,t.article||t.articleKey)}${skuOperationalStatus(t)}${scoreChip(t.focusScore||0)}${trafficBadges(t,"нет")}</div>\n      </div>\n      <button class="btn ghost" data-close-modal>Закрыть</button>\n    </div>\n\n    <div class="kv-3">\n      <div class="card subtle">\n        <h3>Результат</h3>\n        ${metricRow("План Feb 26",fmt.int(t.planFact?.planFeb26Units))}\n        ${metricRow("Факт Feb 26",fmt.int(t.planFact?.factFeb26Units))}\n        ${metricRow("Выполнение",fmt.pct(t.planFact?.completionFeb26Pct),(t.planFact?.completionFeb26Pct||0)<.8?"danger-text":"")}\n        ${metricRow("WB маржа",fmt.pct(t.wb?.marginPct),(t.wb?.marginPct||0)<0?"danger-text":"")}\n        ${metricRow("Ozon маржа",fmt.pct(t.ozon?.marginPct),(t.ozon?.marginPct||0)<0?"danger-text":"")}\n      </div>\n      <div class="card subtle">\n        <h3>Почему в фокусе</h3>\n        ${metricRow("Owner",escapeHtml(ownerName(t)||"Не закреплён"))}\n        ${metricRow("WB остаток",fmt.int(t.wb?.stock),(t.wb?.stock||0)<=50?"warn-text":"")}\n        ${metricRow("Ozon остаток",fmt.int(t.ozon?.stock),(t.ozon?.stock||0)<=50?"warn-text":"")}\n        ${metricRow("Возвраты WB",fmt.pct(t.returns?.wbPct),(t.returns?.wbPct||0)>=.05?"warn-text":"")}\n        ${metricRow("Возвраты Ozon",fmt.pct(t.returns?.ozonPct),(t.returns?.ozonPct||0)>=.05?"warn-text":"")}\n        <div class="note-box">${escapeHtml(t.focusReasons||"Нет явной причины в текущем срезе.")}</div>\n      </div>\n      <div class="card subtle">\n        <h3>Что делаем</h3>\n        <div class="badge-stack">${l?taskPriorityBadge(l):""}${l?taskStatusBadge(l):""}${l?taskTypeBadge(l):""}</div>\n        <div class="note-box">${escapeHtml(l?.nextAction||"Активной задачи пока нет.")}</div>\n        <div class="metric-row"><span>Следующий срок</span><strong>${escapeHtml(l?.due||"—")}</strong></div>\n        <div class="metric-row"><span>Внешний трафик</span><strong>${t?.flags?.hasExternalTraffic?"Есть":"Нет"}</strong></div>\n      </div>\n    </div>\n\n    <div class="two-col" style="margin-top:14px">\n      <div class="card">\n        <h3>Задачи по SKU</h3>\n        <div class="list">${r.length?r.map(renderTaskCard).join(""):'<div class="empty">По этому SKU задач ещё нет</div>'}</div>\n      </div>\n      <div class="card">\n        <h3>Добавить задачу</h3>\n        <form id="manualTaskForm" class="form-grid compact">\n          <input type="hidden" name="articleKey" value="${escapeHtml(e)}">\n          <input name="title" placeholder="Что делаем" required>\n          <select name="type">${Object.entries(TASK_TYPE_META).map(([e,t])=>`<option value="${e}">${escapeHtml(t)}</option>`).join("")}</select>\n          <select name="priority">${Object.entries(PRIORITY_META).map(([e,t])=>`<option value="${e}">${escapeHtml(t.label)}</option>`).join("")}</select>\n          <select name="platform">\n            <option value="cross">Общий контур</option>\n            <option value="wb">РОП WB</option>\n            <option value="ozon">РОП Ozon</option>\n            <option value="retail">ЯМ / Летуаль / Магнит / ЗЯ</option>\n          </select>\n          <input name="owner" placeholder="Owner" value="${escapeHtml(ownerName(t)||"")}">\n          <input name="due" type="date" value="${plusDays(3)}">\n          <textarea name="nextAction" rows="3" placeholder="Следующее действие и что считаем результатом"></textarea>\n          <button class="btn primary" type="submit">Добавить задачу</button>\n        </form>\n      </div>\n    </div>\n\n    <div class="modal-grid-3">\n      <div class="card">\n        <div class="modal-section-title">\n          <div>\n            <h3>Owner и зона ответственности</h3>\n            <p class="small muted">Закрепление по SKU и короткая пометка, если owner меняется.</p>\n          </div>\n          <span class="owner-badge">${escapeHtml(ownerName(t)||"Не закреплён")}</span>\n        </div>\n        <datalist id="skuOwnerList">${o.map(e=>`<option value="${escapeHtml(e)}"></option>`).join("")}</datalist>\n        <form id="ownerForm" class="form-grid compact">\n          <input name="ownerName" list="skuOwnerList" placeholder="Кто owner" value="${escapeHtml(ownerName(t)||"")}" required>\n          <input name="ownerRole" placeholder="Роль / зона" value="${escapeHtml(t?.owner?.registryStatus||"Owner SKU")}">\n          <textarea name="note" rows="3" placeholder="Что важно по закреплению / передаче SKU"></textarea>\n          <button class="btn" type="submit">Сохранить owner</button>\n        </form>\n        <div class="team-note">Командный режим: ${escapeHtml(state.team.note||"Локальный режим")}</div>\n      </div>\n      <div class="card">\n        <div class="modal-section-title">\n          <div>\n            <h3>Журнал решений</h3>\n            <p class="small muted">То, что уже согласовали или ждёт подтверждения руководителя.</p>\n          </div>\n          ${badge(`${fmt.int(i.length)} записей`,i.length?"info":"")}\n        </div>\n        <div class="small-stack">${i.length?i.map(e=>`\n          <div class="decision-item">\n            <div class="head">\n              <strong>${escapeHtml(e.title)}</strong>\n              <div class="badge-stack">${taskStatusBadge(e)}${e.owner?badge(e.owner,"info"):""}</div>\n            </div>\n            <div class="muted small">${escapeHtml(e.decision||"Решение не заполнено")}</div>\n            <div class="meta-line" style="margin-top:8px"><span class="muted small">Срок ${escapeHtml(e.due||"—")}</span><span class="muted small">${escapeHtml(e.createdBy||"Команда")}</span></div>\n          </div>\n        `).join(""):'<div class="empty">Решений пока нет</div>'}</div>\n      </div>\n      <div class="card">\n        <div class="modal-section-title">\n          <div>\n            <h3>Добавить решение</h3>\n            <p class="small muted">Фиксируем не обсуждение, а итог: что решили, кто owner, какой срок.</p>\n          </div>\n        </div>\n        <form id="decisionForm" class="form-grid compact">\n          <input name="title" placeholder="Короткий заголовок решения" required>\n          <input name="owner" placeholder="Кто ведёт решение" value="${escapeHtml(ownerName(t)||"")}">\n          <select name="status">${Object.entries(TASK_STATUS_META).map(([e,t])=>`<option value="${e}" ${"waiting_decision"===e?"selected":""}>${escapeHtml(t.label)}</option>`).join("")}</select>\n          <input name="due" type="date" value="${plusDays(3)}">\n          <textarea name="decision" rows="4" placeholder="Что именно решили / что ещё нужно подтвердить" required></textarea>\n          <button class="btn" type="submit">Сохранить решение</button>\n        </form>\n      </div>\n    </div>\n\n    <div class="two-col" style="margin-top:14px">\n      <div class="card">\n        <h3>Комментарии и апдейты</h3>\n        <div class="list">${s.length?s.map(e=>`\n          <div class="comment-item">\n            <div class="head"><strong>${escapeHtml(e.author||"Команда")}</strong><div class="badge-stack">${commentTypeChip(e.type)}${badge(e.team||"Команда")}</div></div>\n            <div class="muted small">${fmt.date(e.createdAt)}</div>\n            <p>${escapeHtml(e.text)}</p>\n          </div>\n        `).join(""):'<div class="empty">Комментариев пока нет</div>'}</div>\n      </div>\n      <div class="card">\n        <h3>Добавить апдейт</h3>\n        <form id="commentForm" class="form-grid compact">\n          <input type="hidden" name="articleKey" value="${escapeHtml(e)}">\n          <input name="author" placeholder="Кто пишет" value="${escapeHtml(state.team.member.name||ownerName(t)||"Команда")}" required>\n          <select name="type">\n            <option value="signal">Сигнал</option>\n            <option value="risk">Риск</option>\n            <option value="focus">Фокус</option>\n            <option value="idea">Идея</option>\n          </select>\n          <textarea name="text" rows="5" placeholder="Коротко: что случилось, что делаем, что нужно от других" required></textarea>\n          <button class="btn" type="submit">Сохранить апдейт</button>\n        </form>\n      </div>\n    </div>\n  `,a.classList.add("open"),n.querySelector("#manualTaskForm").addEventListener("submit",async t=>{t.preventDefault();const n=new FormData(t.currentTarget);await createManualTask({articleKey:e,title:n.get("title"),type:n.get("type"),priority:n.get("priority"),platform:n.get("platform"),owner:n.get("owner"),due:n.get("due"),nextAction:n.get("nextAction")}),renderSkuModal(e),rerenderCurrentView()}),n.querySelector("#ownerForm").addEventListener("submit",async t=>{t.preventDefault();const n=new FormData(t.currentTarget);await upsertOwnerAssignment({articleKey:e,ownerName:n.get("ownerName"),ownerRole:n.get("ownerRole"),note:n.get("note")}),renderSkuModal(e),rerenderCurrentView()}),n.querySelector("#decisionForm").addEventListener("submit",async t=>{t.preventDefault();const n=new FormData(t.currentTarget);await createDecision({articleKey:e,title:n.get("title"),decision:n.get("decision"),owner:n.get("owner"),status:n.get("status"),due:n.get("due")}),renderSkuModal(e),rerenderCurrentView()}),n.querySelector("#commentForm").addEventListener("submit",async t=>{t.preventDefault();const n=new FormData(t.currentTarget);await createComment({articleKey:e,author:n.get("author"),team:teamMemberLabel(),type:n.get("type"),text:n.get("text")}),renderSkuModal(e)})}function deriveLaunchStatus(e){return ownerName(e)?e?.flags?.negativeMargin||e?.flags?.toWork?"Нужна корректировка экономики":totalSkuStock(e)<=0?"Ждём поставку":e?.flags?.hasExternalTraffic?"В работе":"Готовим трафик":"Нужен owner"}function deriveLaunchPhase(e){return ownerName(e)?totalSkuStock(e)<=0?"supply":e?.flags?.hasExternalTraffic?e?.flags?.negativeMargin||e?.flags?.toWork?"economy":"scale":"content":"owner"}function launchPhaseMeta(e){const t={owner:{label:"Нужен owner",tone:"warn"},supply:{label:"Поставка",tone:"warn"},content:{label:"Контент / трафик",tone:"info"},economy:{label:"Экономика",tone:"danger"},scale:{label:"Масштабирование",tone:"ok"}};return t[e]||t.content}function normalizeLaunchItem(e={}){return{id:e.id||stableId("launch",e.articleKey||e.name||e.title||""),articleKey:e.articleKey||"",name:e.name||e.title||"Новинка",reportGroup:e.reportGroup||e.segment||"Продукт",subCategory:e.subCategory||e.category||"—",launchMonth:e.launchMonth||state.dashboard?.dataFreshness?.launchPlanHorizon||"Текущий фокус",status:e.status||"Статус не указан",phase:e.phase||"content",owner:e.owner||"",production:e.production||"",plannedRevenue:numberOrZero(e.plannedRevenue),targetCost:numberOrZero(e.targetCost),externalTraffic:e.externalTraffic||"без внешнего трафика",activeTasks:numberOrZero(e.activeTasks),blockers:Array.isArray(e.blockers)?e.blockers.filter(Boolean):[]}}function buildLaunchItemsFromSkus(){return state.skus.filter(e=>"GROWTH"===String(e?.segment||"").toUpperCase()||String(e?.status||"").toLowerCase().includes("нов")).map(e=>{const t=deriveLaunchPhase(e),n=[];return ownerName(e)||n.push("не назначен owner"),totalSkuStock(e)<=0&&n.push("нет остатка"),e?.flags?.hasExternalTraffic||n.push("нет внешнего трафика"),e?.flags?.negativeMargin&&n.push("маржа в риске"),e?.flags?.highReturn&&n.push("высокие возвраты"),normalizeLaunchItem({articleKey:e.articleKey,name:e.name||e.article||e.articleKey,reportGroup:e.segment||"GROWTH",subCategory:e.category||"—",launchMonth:state.dashboard?.dataFreshness?.launchPlanHorizon||"Текущий фокус",status:deriveLaunchStatus(e),phase:t,owner:ownerName(e),production:totalSkuStock(e)>0?`Остаток ${fmt.int(totalSkuStock(e))}`:"Без остатка",plannedRevenue:monthRevenue(e),targetCost:0,externalTraffic:externalTrafficLabel(e),activeTasks:getSkuControlTasks(e.articleKey).filter(isTaskActive).length,blockers:n})}).sort((e,t)=>t.activeTasks-e.activeTasks||t.plannedRevenue-e.plannedRevenue||e.name.localeCompare(t.name,"ru"))}function getLaunchItems(){return(Array.isArray(state.launches)&&state.launches.length?state.launches.map(normalizeLaunchItem):buildLaunchItemsFromSkus()).slice(0,24)}function renderLaunches(){const e=document.getElementById("view-launches"),t=getLaunchItems(),n=t.map(e=>`\n    <div class="list-item">\n      <div class="head">\n        <div>\n          <strong>${e.articleKey?linkToSku(e.articleKey,e.name||"Новинка"):escapeHtml(e.name||"Новинка")}</strong>\n          <div class="muted small">${escapeHtml(e.reportGroup||"—")} · ${escapeHtml(e.subCategory||"—")}</div>\n        </div>\n        ${badge(e.launchMonth||"—","info")}\n      </div>\n      <div class="badge-stack">${badge(launchPhaseMeta(e.phase).label,launchPhaseMeta(e.phase).tone)}${e.production?badge(e.production):""}${e.owner?badge(e.owner,"info"):badge("Без owner","warn")}</div>\n      <div class="muted small" style="margin-top:8px">${escapeHtml(e.status||"Статус не указан")}</div>\n      <div class="muted small" style="margin-top:8px">План выручки: ${fmt.money(e.plannedRevenue)} · Трафик: ${escapeHtml(e.externalTraffic||"—")}</div>\n      <div class="badge-stack" style="margin-top:8px">${badge(`${fmt.int(e.activeTasks||0)} активн. задач`,e.activeTasks?"warn":"ok")}${(e.blockers||[]).slice(0,2).map(e=>badge(e,"warn")).join("")}</div>\n    </div>\n  `).join("");e.innerHTML=`\n    <div class="section-title">\n      <div>\n        <h2>Новинки и pipeline</h2>\n        <p>Собрала product-layer по SKU роста и новинкам: карточка, owner, экономика, трафик и текущие блокеры в одном месте.</p>\n      </div>\n      <div class="badge-stack">${badge(`${fmt.int(t.length)} SKU в запуске`,t.length?"info":"warn")}${badge(`${fmt.int(t.filter(e=>!e.owner).length)} без owner`,t.filter(e=>!e.owner).length?"warn":"ok")}${badge(`${fmt.int(t.filter(e=>e.activeTasks).length)} с активными задачами`,t.filter(e=>e.activeTasks).length?"warn":"ok")}</div>\n    </div>\n    <div class="card">\n      <div class="pipeline-strip">\n        <span>Owner</span><span>Поставка</span><span>Контент</span><span>Трафик</span><span>Экономика</span><span>Масштаб</span>\n      </div>\n      <div class="list" style="margin-top:14px">${n||'<div class="empty">Нет новинок в текущем срезе</div>'}</div>\n    </div>\n  `}function renderLaunchControl(){const e=document.getElementById("view-launch-control"),t=getLaunchItems(),n=["owner","supply","content","economy","scale"].map(e=>({key:e,meta:launchPhaseMeta(e),items:t.filter(t=>t.phase===e)})).filter(e=>e.items.length);e.innerHTML=`\n    <div class="section-title">\n      <div>\n        <h2>Запуск новинок</h2>\n        <p>Отдельный экран запуска: сверху видно, где именно сейчас блокируется запуск, а ниже можно пройтись по SKU как по чек-листу.</p>\n      </div>\n      <div class="badge-stack">\n        ${badge(`${fmt.int(t.length)} в запуске`,t.length?"info":"warn")}\n        ${badge(`${fmt.int(t.filter(e=>"owner"===e.phase).length)} ждут owner`,t.filter(e=>"owner"===e.phase).length?"warn":"ok")}\n        ${badge(`${fmt.int(t.filter(e=>"content"===e.phase).length)} ждут трафик`,t.filter(e=>"content"===e.phase).length?"warn":"ok")}\n      </div>\n    </div>\n\n    <div class="grid cards" style="margin-top:14px">\n      ${n.map(e=>`\n        <div class="card kpi">\n          <div class="label">${escapeHtml(e.meta.label)}</div>\n          <div class="value">${fmt.int(e.items.length)}</div>\n          <div class="hint">SKU в этой фазе запуска</div>\n          <div class="badge-stack" style="margin-top:10px">${e.items.slice(0,3).map(e=>badge(e.owner||e.name,e.owner?"":"warn")).join("")}</div>\n        </div>\n      `).join("")||'<div class="card"><div class="empty">Нет SKU в запуске</div></div>'}\n    </div>\n\n    ${n.map(e=>`\n      <div class="card" style="margin-top:14px">\n        <div class="section-subhead">\n          <div>\n            <h3>${escapeHtml(e.meta.label)}</h3>\n            <p class="small muted">Что надо закрыть на этом этапе, чтобы SKU не зависал между отделами.</p>\n          </div>\n          ${badge(`${fmt.int(e.items.length)} SKU`,e.meta.tone)}\n        </div>\n        <div class="list">\n          ${e.items.map(e=>`\n            <div class="list-item">\n              <div class="head">\n                <div>\n                  <strong>${e.articleKey?linkToSku(e.articleKey,e.name||"Новинка"):escapeHtml(e.name||"Новинка")}</strong>\n                  <div class="muted small">${escapeHtml(e.subCategory||"—")}</div>\n                </div>\n                <div class="badge-stack">${e.owner?badge(e.owner,"info"):badge("Без owner","warn")}${badge(`${fmt.int(e.activeTasks||0)} задач`,e.activeTasks?"warn":"ok")}</div>\n              </div>\n              <div class="check-list">\n                <div class="check-item"><strong>1.</strong><span>${ownerName(getSku(e.articleKey))?"Owner назначен":"Назначить owner и зону ответственности"}</span></div>\n                <div class="check-item"><strong>2.</strong><span>${totalSkuStock(getSku(e.articleKey))>0?`Остаток есть: ${fmt.int(totalSkuStock(getSku(e.articleKey)))}`:"Проверить поставку и доступность SKU"}</span></div>\n                <div class="check-item"><strong>3.</strong><span>${getSku(e.articleKey)?.flags?.hasExternalTraffic?`Трафик есть: ${escapeHtml(e.externalTraffic)}`:"Подготовить запуск трафика / контента"}</span></div>\n                <div class="check-item"><strong>4.</strong><span>${(e.blockers||[]).length?`Блокеры: ${escapeHtml(e.blockers.join(", "))}`:"Критичных блокеров не видно"}</span></div>\n              </div>\n            </div>\n          `).join("")}\n        </div>\n      </div>\n    `).join("")}\n  `}function renderMeetings(){const e=document.getElementById("view-meetings"),t=(state.meetings||[]).map(e=>{const t=String(e.id||"").startsWith("weekly")?"Weekly":String(e.id||"").startsWith("monthly")?"Monthly":"PMR",n=Array.isArray(e.outputs)?e.outputs.join(" · "):e.outputs||"—",a=Array.isArray(e.participants)?e.participants.join(", "):"—";return`\n      <div class="card meeting-card">\n        <div class="head">\n          <div>\n            <h3>${escapeHtml(e.title||"Встреча")}</h3>\n            <div class="muted small">${escapeHtml(e.cadence||"—")} · ${escapeHtml(e.duration||"—")}</div>\n          </div>\n          ${badge(t)}\n        </div>\n        <p>${escapeHtml(e.question||"—")}</p>\n        <div class="muted small"><strong>Участники:</strong> ${escapeHtml(a)}</div>\n        <div class="muted small" style="margin-top:8px"><strong>Выход:</strong> ${escapeHtml(n)}</div>\n      </div>\n    `}).join("");e.innerHTML=`\n    <div class="section-title">\n      <div>\n        <h2>Ритм работы</h2>\n        <p>Weekly / Monthly / PMR должны рождать задачи с owner и сроком — без этого портал не будет живым.</p>\n      </div>\n    </div>\n    <div class="grid cards-2">${t||'<div class="empty">Нет карты встреч</div>'}</div>\n  `}function getDocumentGroupsFiltered(){const e=String(state.docFilters.search||"").trim().toLowerCase();return(state.documents?.groups||[]).map(t=>({...t,items:(t.items||[]).filter(n=>{const a=[t.title,n.title,n.description,n.type,n.filename].filter(Boolean).join(" ").toLowerCase();return("all"===state.docFilters.group||t.title===state.docFilters.group)&&!(e&&!a.includes(e))})})).filter(e=>e.items.length)}