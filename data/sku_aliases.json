{
  "schema": "sku-api-aliases-v1",
  "aliases": [],
  "columns": [
    "target_sku",
    "platform",
    "api_sku",
    "status",
    "note"
  ],
  "description": "Optional local mapping for API SKU values that must be attached to portal articleKey values. Prefer the Google Sheet tab dim_sku_aliases for team-managed mappings."
}
