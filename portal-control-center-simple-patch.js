(function () {
  if (window.__ALTEA_TASK_SIMPLE_PATCH_20260422__) return;
  window.__ALTEA_TASK_SIMPLE_PATCH_20260422__ = true;

  // Legacy simple patch layer is intentionally minimal now.
  // The active control-center and form-refine layers already provide
  // the split marketplace tabs and task controls.
})();